# LLM & API interactions
# Together.ai is OPTIONAL - only needed if not using OpenCode Zen
together==0.2.11  # Fallback LLM (optional, ~$0.0008/1K tokens)
requests==2.31.0  # HTTP client
pydantic==2.5.0   # Data validation

# Text-to-Speech
elevenlabs==0.2.26

# Video generation
manim==0.18.0
moviepy==1.0.3
pillow==10.1.0

# Instagram posting
instagrapi==2.0.0

# Data fetching & APIs
beautifulsoup4==4.12.2  # Web scraping
feedparser==6.0.10      # RSS feed parsing
praw==7.7.0             # Reddit API (Python Reddit API Wrapper)
wikipedia==1.4.0        # Wikipedia API client (OPTIONAL - requests does the job)

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1
loguru==0.7.2

# Data processing
pandas==2.1.3
numpy==1.26.2

